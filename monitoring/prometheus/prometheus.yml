global:
  scrape_interval: 15s      # собирать метрики каждые 15 секунд
  evaluation_interval: 15s

scrape_configs:
  - job_name: "horizon-detection-api"
    static_configs:
      - targets: ["api:8000"]   # имя сервиса из docker-compose
    metrics_path: /metrics
